# 战术终端 PWA · 装到手机主屏幕 部署说明

这次给你的是7个文件：

```
index.html        ← 战术终端本体（就是之前那个HTML工具，加了几行"能装到手机"的配置）
manifest.json      ← 告诉手机"这是个App"的说明文件
sw.js              ← Service Worker，让它能像App一样独立运行
icon-192.png       ← 图标（Android用）
icon-512.png       ← 图标（Android用，高清）
icon-180.png       ← 图标（iPhone用）
favicon-32.png     ← 网页小图标
```

## 先说一个关键点：为什么不能直接打开文件

跟之前下载HTML文件直接双击打开不一样，**PWA必须通过一个真实的网址(https://)才能"装到主屏幕"**，直接打开本地文件浏览器是不允许的（这是浏览器自己的安全限制，所有网站的PWA功能都受这个限制，不是咱们这个工具特殊）。

所以需要先把这7个文件放到一个能通过网址访问的地方。你已经会用GitHub了（部署机器人的时候用过），最省事的办法就是用 **GitHub Pages**——GitHub免费提供的静态网页托管服务。

---

## 第一步：新建一个仓库

跟部署机器人时一样的操作：
1. GitHub右上角 `+` → `New repository`
2. 名字随便起，比如 `sniper-terminal`
3. **这次建议选 Public（公开）**，因为GitHub Pages的免费额度只对公开仓库完全免费；这个工具本身不含任何密钥，公开也没有安全问题
4. `Create repository`

## 第二步：把7个文件传上去

`Add file` → `Upload files`，把这7个文件一次性拖进去，`Commit changes`。

## 第三步：开启 GitHub Pages

1. 进这个仓库 → `Settings` → 左侧菜单找 `Pages`
2. "Build and deployment" 下面的 `Source` 选择 `Deploy from a branch`
3. `Branch` 选 `main`，文件夹选 `/ (root)`，点 `Save`
4. 等大概1分钟，刷新页面，顶部会出现一行绿字，类似：
   ```
   Your site is live at https://你的用户名.github.io/sniper-terminal/
   ```
   这个网址就是你以后要用的地址，记下来或者直接收藏。

## 第四步：手机上添加到主屏幕

用手机浏览器（iPhone用**Safari**，安卓用**Chrome**，注意不是微信内置浏览器，微信浏览器不支持这个功能）打开上一步拿到的网址，然后：

- **iPhone**：点底部中间的"分享"图标（方框+箭头）→ 下拉找到 `添加到主屏幕` → 右上角点`添加`
- **安卓Chrome**：点右上角三个点菜单 → `添加到主屏幕` 或者会自动弹出"安装应用"的提示条，直接点安装

完成后主屏幕会出现一个绿色的图标（配色跟工具本身一致），点开就是全屏运行，没有浏览器的网址栏和按钮，跟真App的体验基本一致。

---

## 之后想更新内容怎么办

以后如果你想改工具里的某些参数或样式，只要把新版本的文件重新上传覆盖到这个仓库（`Add file → Upload files`，同名文件会提示覆盖，确认即可），手机上已经装好的图标**不需要重新添加**，下次打开会自动读取最新版本（可能需要完全退出重新打开一次才会刷新，这是Service Worker缓存机制导致的，属于正常现象）。

## 老实说清楚的几点限制

- 这终究是"网页装到主屏幕"，不是真正编译出来的原生App，不会出现在App Store/应用商店里搜到，也没法在手机系统设置的"应用管理"里看到（iOS上会算在Safari的存储里，安卓上会算在Chrome里）。
- 部分iOS系统更新后，Safari的PWA支持细节可能有调整（比如角标提醒、后台运行能力），这是苹果自己平台策略的事，不是这份代码能控制的。
- 里面的实时行情获取、微信提醒这些功能逻辑跟之前完全一样，没有任何改动，只是多了"能装图标"这一层外壳。
